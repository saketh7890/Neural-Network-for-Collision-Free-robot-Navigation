# Neural-Network-for-Collision-Free-robot-Navigation
This project implements a simulation in which a robot learns to move through a maze without colliding with the walls. A neural network is trained on sensor readings and action data to predict whether a movement is safe or leads to a collision.
